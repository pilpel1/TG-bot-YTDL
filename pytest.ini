[pytest]
# הטסטים יושבים ב-tests/ אבל מייבאים מודולים משורש הריפו (import utils
# וכו'). בלי pythonpath, pytest מוסיף ל-sys.path רק את tests/ עצמה, ולכן
# `pytest` סתם (בלי python -m) היה נופל על ModuleNotFoundError.
pythonpath = .
testpaths = tests
